<Page x:Class="SoftwareKobo.ACGNews.Views.NotificationView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:SoftwareKobo.ACGNews.Views"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:control="using:SoftwareKobo.ACGNews.Controls"
      mc:Ignorable="d">
    <Grid>
        <ProgressBar HorizontalAlignment="Stretch"
                     x:Name="LoadingProgressBar"
                     Visibility="Collapsed"
                     VerticalAlignment="Top"
                     IsIndeterminate="True" />
        <TextBlock x:Name="LoadingMessage"
                   TextAlignment="Left"
                   VerticalAlignment="Top"
                   Margin="8,0,0,0" />
        <TextBlock x:Name="LocalTime"
                   VerticalAlignment="Top"
                   TextAlignment="Right"
                   Margin="0,0,8,0"
                   Loaded="LocalTime_OnLoaded" />
        <Grid x:Name="ToastPromptContainer"></Grid>
    </Grid>
</Page>